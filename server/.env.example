# SERVER
PORT=8080
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# TOSS (샌드박스 기준, 실제 값으로 교체)
TOSS_CLIENT_ID=your_client_id
TOSS_CLIENT_SECRET=your_client_secret
# AES-256-GCM 복호화 키(Base64 or hex). 문서에서 제공되는 원문 키 형태에 맞춰 KEY_FORMAT 지정
TOSS_DECRYPTION_KEY=your_256bit_key_in_hex_or_base64
TOSS_KEY_FORMAT=hex # hex | base64

# OAuth2 교환/유저조회 엔드포인트 (앱인토스 문서 기준)
TOSS_TOKEN_URL=https://api.tosspayments.com/api-partner/v1/apps-in-toss/user/oauth2/token
TOSS_ME_URL=https://api.tosspayments.com/api-partner/v1/apps-in-toss/user/oauth2/me

# 우리 앱 베이스 URL (리다이렉트/링크 생성 시)
APP_BASE_URL=http://localhost:5173
